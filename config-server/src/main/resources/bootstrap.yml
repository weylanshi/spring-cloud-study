server:
  port: 8888
spring:
  application:
    name: loc-config
  cloud:
    config:
      server:
        git:
          username: weylanS@outlook.com
          password:
          uri: https://github.com/weylanshi/spring-cloud-study.git
          search-paths: loc-config
          force-pull: false #强制更新
          basedir: /var/config
